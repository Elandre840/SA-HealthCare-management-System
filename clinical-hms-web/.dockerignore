# Git
.git
.gitignore

# Dependencies — installed fresh inside the image by npm ci
node_modules/

# Build output — not needed in the dev image
dist/
dist-ssr/

# Environment files — injected at runtime via docker-compose environment block
.env
.env.*

# Editor / OS
.vscode/
.idea/
.cursor/
.DS_Store
*.local

# Docs
README.md
